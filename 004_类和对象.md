	````java
  //
  Student s1 = new Student();
		Student[] arr = new Student[3];//只分配了arr的内存   参考int[] a =new int[3];
		for (int i = 0; i < arr.length; i++) {
			arr[i] = new Student();//一个学生对象用( )   参考 Student s = new Student() //只分配了arr[i]的内存
		}
		
////		s1.no = "2018001001 ";
//		s1.setNo("2018001001");//最常见
////		s1.name = "zhangsan";
		System.out.println( arr == null);
//		arr[0].setName("zhangsan");  //arr数组名字，     arr[0]数组首元素
		s1.setStudent("1","haha");
		
		s1.print();
		System.out.println(s1.getName());
		//用对象调用函数，类似与数组，访问类的方法，可通过类的对象
		//将数据及操作封装为类，以类的对象作为程序的基本元素，通过向对象发送消息（调用对象的方法）完成各种功能
		//对象为基本元素，数据安全（存在访问权限），维护性较小，程序=对象+对象。。。。。
		//变量:基本数据类型
		//对象:引用数据类型
		//private 类的外部无法访问
		//最严格与最开放的封装
		//定义对象同时，就分配了所有属性成员的内存，但没方法。
		//方法：代码区;  变量：堆或栈区
    ````
	
